# Extended UML and BCE Diagrams

The PNG files are submission-ready figures. Each PNG has a neighbouring Mermaid `.mmd` source so the diagram remains editable and reviewable in Git.

- `use-case-extended`: role-to-capability overview for the baseline plus extension flows.
- `bce-extended`: consolidated Boundary-Control-Entity architecture.
- `sequence-tassel-count`: authenticated upload and tassel-count flow.
- `sequence-disease-review`: Farmer screening and assigned Agronomist review flow.
- `sequence-dual-analysis`: complete dual-pipeline technical sequence.
- `../../database/erd-extended`: database entities and cardinalities after migrations 001-004.

These diagrams supplement the original per-story BCE and sequence diagrams. No original diagram source is replaced.
