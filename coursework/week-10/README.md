# Week 10 Archive

`submission/` preserves the Week 10 submission package and its original file
names. `notes/` contains planning and development context from that period.

Some binary documents also have categorized copies under `docs/` because those
locations are easier for an assessor to discover. Current source code and model
evidence always take precedence over statements in this historical package.

