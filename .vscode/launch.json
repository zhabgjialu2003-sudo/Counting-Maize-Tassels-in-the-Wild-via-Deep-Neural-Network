{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Maize Detector / 运行玉米检测系统",
      "type": "debugpy",
      "request": "launch",
      "program": "${workspaceFolder}\\backend\\server.py",
      "cwd": "${workspaceFolder}",
      "envFile": "${workspaceFolder}\\backend\\.env",
      "console": "integratedTerminal",
      "justMyCode": true
    }
  ]
}
